{
  "name": "pwa-workbox",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "lite-server --baseDir src --port 3200",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.5.4"
  }
}
